abstract model Dates {
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())
}

abstract model ReferenceLink {
  referenceLink String?
}

abstract model Description {
  description String?
}

abstract model CUID {
  id String @id @default(cuid())
}
